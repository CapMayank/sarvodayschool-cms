/** @format */

export const metadata = {
	title: "Admission",
	description:
		"Apply for admissions at Sarvodaya English Higher Secondary School Lakhnadon. Discover our admission process, requirements, and important dates.",
};

export default function AdmissionLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
