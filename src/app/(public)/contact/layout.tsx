/** @format */

export const metadata = {
	title: "Contact",
	description:
		"Get in touch with Sarvodaya School for admission inquiries, academic details, and general assistance. Reach out via phone, email, or visit our campus.",
};

export default function AdmissionLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
