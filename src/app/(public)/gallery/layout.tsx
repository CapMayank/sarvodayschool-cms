/** @format */

export const metadata = {
	title: "Gallery",
	description:
		"View the moments captured at Sarvodaya English Higher Secondary School Lakhnadon.",
};

export default function GalleryLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
